# Third-Party Notice

This project is distributed under the MIT license. It depends on open-source packages
that are license-compatible with MIT distribution.

Fallback formatter/license shortlist (reviewed):

- `prettier`: MIT
- `js-beautify`: MIT
- `eta`: MIT
- `ejs`: Apache-2.0

Run `npm run licenses:report` to generate an installed dependency summary and
`npm run licenses:check` to enforce the allowlist in CI.
